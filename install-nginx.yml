#cloud-config
package_update: true
packages:
  - nginx
runcmd:
  - echo '<html><body><h1>Hello from Azure VM!</h1></body></html>' > /var/www/html/index.html
  - systemctl enable nginx
  - systemctl start nginx